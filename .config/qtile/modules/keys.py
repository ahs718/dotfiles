from libqtile.config import Key
from libqtile.lazy import lazy

import os
import subprocess

# Assign keys and home directory values to variables
mod = "mod4"
control = "control"
shift = "shift"
alt = "mod1"
terminal = "kitty"
home = os.path.expanduser('~')